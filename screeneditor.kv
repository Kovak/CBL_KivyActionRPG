#:kivy 1.3.0


<Tile>:
    size: root.size
    source_image: root.source_image
    Image:
        source: root.test(root.parent.active_frame)
        size: root.size
        pos: root.pos
        mipmap: True